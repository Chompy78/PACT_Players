# z-uploads

Drop-zone folder. Anything placed here is automatically committed and pushed
to this repo's GitHub remote within a few seconds, by a background sync
script running on this machine (see `~/dev/zcold-sync`). Not part of the
site itself — just a convenience for getting files into the repo quickly.
