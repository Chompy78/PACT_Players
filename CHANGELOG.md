# Changelog

What changed, from a reader's/player's point of view. Newest entries on
top. See `AGENTS.md` for what belongs here vs `DECISIONS.md`/`WORKLOG.md`.

## 2026-07-11

- Renamed the arc folders/URLs for a consistent scheme: `A01_prelude` →
  `arc01_prelude`, `A02_arc2` → `arc02_arc2`, `A03_arc3` → `arc03_arc3`.
  Page headings updated to match (e.g. "A01 Prelude" → "Arc 01: Prelude").
  Old URLs are **not** redirected — the site is brand new, so nothing was
  bookmarked yet.
- Added `AGENTS.md` plus a `CHANGELOG.md` / `DECISIONS.md` / `WORKLOG.md`
  logging system, and a CI guardrail that blocks any `content/` change
  containing a `SECRET:`/`GM-ONLY:` marker from deploying. No player-facing
  content changed.
